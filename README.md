# agent-query-api
Un servicio web expuesto mediante una API REST que recibe preguntas en lenguaje natural sobre el estado de un inventario o pedidos, procesa la consulta usando un grafo de LangGraph, consulta los datos mediante una herramienta estructurada bajo MCP (Model Context Protocol) y devuelve una respuesta en formato JSON.
